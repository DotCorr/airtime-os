[main]
# Single-purpose kiosk device: the unprivileged kiosk session must be able to
# configure Wi-Fi from the settings UI, and this image ships no polkit.
auth-polkit=false

[ifupdown]
managed=true
