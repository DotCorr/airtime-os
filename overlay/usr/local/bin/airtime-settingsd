#!/bin/sh
# Tiny local settings server: Wi-Fi picker for first boot, driven by nmcli.
# busybox-extras httpd + static CGI shipped in the image overlay.
set -u
exec busybox-extras httpd -f -p 8800 -h /usr/local/share/airtime-settings
